services:
  web-research:
    build: .
    container_name: uaf-web-research
    ports:
      - "3456:3456"
    env_file: .env
    depends_on:
      searxng:
        condition: service_healthy
    networks:
      - uaf-network
    restart: unless-stopped

  searxng:
    image: searxng/searxng:latest
    container_name: uaf-searxng
    ports:
      - "8888:8080"
    environment:
      - SEARXNG_BASE_URL=http://localhost:8888
      - SEARXNG_SECRET=${SEARXNG_SECRET}
    volumes:
      - ./searxng-settings.yml:/etc/searxng/settings.yml
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - uaf-network
    restart: unless-stopped

networks:
  uaf-network:
    external: true
    name: uaf-network
